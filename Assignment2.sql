USE DEMO;
CREATE TABLE COURSE (
    COURSECODE NUMERIC(30) NOT NULL,
    COURESENAME VARCHAR(30),
    SYLLABUS VARCHAR(60),
    LASTNO NUMERIC(5)
);
INSERT INTO COURSE
            VALUES
            (10, 'ORACLE', 'SQL, PL SQL, JDBC ,JSP', 2),
            (20, 'JAVA', 'JAVA ,CORE JAVA ,SERVLETS ,JSP, EJB', 2);
SELECT * FROM COURSE;
CREATE TABLE BATCH (
    BATCHCODE NUMERIC(5) NOT NULL,
    COURESECODE NUMERIC(5) NOT NULL,
    STARTINGDATE DATE NOT NULL,
    COURSEFEES NUMERIC(10 , 2 ) NOT NULL,
    NETINCOME NUMERIC(10 , 2 ),
    EXPECTEDINCOME NUMERIC(10 , 2 ),
    LASTNO NUMERIC(5)
);
INSERT INTO BATCH
            VALUES
            (20002, 20, '2001-07-01', 180, 24000, 0,1),
            (10002, 10, '1999-01-23', 3, 4000, 2000, 2),
            (10001, 10, '1999-01-11', 2, 3000, 6000, 5),
            (10003, 10, '1999-01-25', 4, 3000,7000, 7);
SELECT * FROM BATCH;
SELECT 
    COURSENAME, BATCHCODE, STARTINGDATE
FROM
    BATCH,
    COURSE
WHERE
    BATCH.COURESECODE = COURSE.COURSECODE;

SELECT 
    COURSENAME, BATCHCODE, STARTINGDATE
FROM
    BATCH,
    COURSE
WHERE
    BATCH.BATCHCODE = COURSE.COURSECODE
        AND BATCH.BATCHCODE = 10001;

SELECT COURSE.COU
